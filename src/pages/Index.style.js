import {
    StyleSheet
} from 'react-native';

import {GlobalColors} from '../global.style';

export var styles = StyleSheet.create({
    container: {
        flex: 1
    },

    content: {
        flex: 1
    }
});

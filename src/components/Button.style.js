import {
    StyleSheet
} from 'react-native';

import {GlobalStyles, GlobalColors, SCREEN_WIDTH} from '../global.style.js'

export var styles = StyleSheet.create({
    button: {
        alignItems: 'center',
        justifyContent: 'center'
    }
});